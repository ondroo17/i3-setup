#!/bin/bash
DEVICE="--"
DEVICE=$(echo -e 'info\nquit' | bluetoothctl | awk '/Name/ {print $2}')

if [[ -z "${DEVICE}" ]]; then
	echo "--"
	echo "--"
	echo ""
else
	echo "$DEVICE"
	echo "$DEVICE"
	echo ""
fi


if [[ "${BLOCK_BUTTON}" -eq 1 ]]; then
 blueman-manager
fi